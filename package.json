{
  "name": "backend",
  "version": "1.0.0",
  "description": "Backend for workforce project using json-server",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --port 5000"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}